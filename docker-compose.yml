version: '3.1'

services:

  db:
    image: postgres
    restart: always
    volumes:
      - ./pg-data:/var/lib/postgresql/data
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: admin
      POSTGRES_USER: admin
      POSTGRES_DB: todo